{% set name = "lalsuite" %}
{% set version = "7.13" %}

# sub-package versions
{% set lal_version = "7.3.0" %}
{% set lalframe_version = "3.0.0" %}
{% set lalmetaio_version = "4.0.0" %}
{% set lalsimulation_version = "5.0.0" %}
{% set lalburst_version = "2.0.0" %}
{% set lalinspiral_version = "4.0.0" %}
{% set lalinference_version = "4.1.2" %}
{% set lalpulsar_version = "6.0.0" %}
{% set lalapps_version = "9.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://software.igwn.org/sources/source/lalsuite/{{ name }}-{{ version }}.tar.gz
  sha256: cfdf33cb64cfbfd2cba83ee063d556ffb6c60525dea91042156d467bff9c41bf

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install -vv .

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools >=38.3.0
  run:
    - python >=3.6
    - lal {{ lal_version }}.*
    - lalframe {{ lalframe_version }}.*
    - lalmetaio {{ lalmetaio_version }}.*
    - lalsimulation {{ lalsimulation_version }}.*
    - lalburst {{ lalburst_version }}.*
    - lalinspiral {{ lalinspiral_version }}.*
    - lalpulsar {{ lalpulsar_version }}.*
    - lalinference {{ lalinference_version }}.*
    - lalapps {{ lalapps_version }}.*

test:
  imports:
    - lal
    - lalframe
    - lalmetaio
    - lalsimulation
    - lalburst
    - lalinspiral
    - lalpulsar
    - lalinference
    - lalapps
  requires:
    - pip
    - pytest
  source_files:
    - setup.cfg
  commands:
    # run_test.py checks that the versions are up-to-date
    # show metadata
    - python -m pip show {{ name }}
    # check requirements
    - python -m pip check {{ name }}

about:
  home: https://wiki.ligo.org/Computing/LALSuite
  doc_url: https://lscsoft.docs.ligo.org/lalsuite/
  dev_url: https://git.ligo.org/lscsoft/lalsuite.git
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: COPYING
  summary: LSC Algorithm Library Suite
  description: |
    The LSC Algorithm Library Applications for gravitational wave data
    analysis.  This package contains applications that are built on tools
    in the LSC Algorithm Library.

extra:
  recipe-maintainers:
    - duncanmmacleod
    - skymoo
